table:
  name: user
  schema: public
array_relationships:
  - name: entries
    using:
      foreign_key_constraint_on:
        column: user_id
        table:
          name: entry
          schema: public
